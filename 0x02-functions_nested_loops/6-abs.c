#include<stdio.h>
#include"main.h"

/**
* _abs - value of an integer
* integar n:  is an integer
* Return: integer
*/

int _abs(int);
{
	if (n > 0)
	{
		return (n);
	}
	else if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
